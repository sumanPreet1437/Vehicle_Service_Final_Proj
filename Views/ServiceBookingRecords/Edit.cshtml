@model Vehicle_Service_Final_Proj.Models.ServiceBookingRecord

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ServiceBookingRecord</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ServiceId" class="control-label"></label>
                <select asp-for="ServiceId" class="form-control" asp-items="ViewBag.ServiceId"></select>
                <span asp-validation-for="ServiceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VehicleOwnerId" class="control-label"></label>
                <select asp-for="VehicleOwnerId" class="form-control" asp-items="ViewBag.VehicleOwnerId"></select>
                <span asp-validation-for="VehicleOwnerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
